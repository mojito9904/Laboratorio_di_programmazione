package Laboratorio_di_programmazione.Secondo_parziale.parziale_2021;

public class Main {
    public static void main(String arg[]){

        /* ============================================================
           MAIN PER IL PRIMO METODO
           ============================================================ */
        /*
        // Array bidimensionale di stringhe
        String [][] a = {
            {"abc","bb","d"},
            {"rkr","rc"},
            {"mp","aqsfg","ats","ar"}
        };

        // Array dei caratteri da confrontare
        char[] c = {'b','r','a'}; 

        // Soglia numerica utilizzata dal metodo
        int k = 4;

        // Stampa il risultato booleano del metodo1
        System.out.println(metodo.metodo1(a, c, k));
        */


        /* ============================================================
           MAIN PER IL SECONDO METODO (versione iterativa e ricorsiva)
           ============================================================ */
        /*
        String[] a = {"apqpwp","gwwg","jxxh","y","qq","rzrr"};
        char[]  c = {'p','g','x','z','q','r'};

        // Versione non ricorsiva:
        // String[] b = metodo.metodo2(a, c);

        // Versione ricorsiva:
        String[] b = metodo.metodo2_ricorsivo(a, c);

        // Stampa del risultato (array di stringhe)
        for (int i = 0; i < b.length; i++) {
            System.out.println(b[i]);
        }
        */
    }
}
